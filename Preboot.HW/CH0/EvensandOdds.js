function EO(arr){
    var arr2 = 0;
    for (i = 0; i < arr.length; i++){
        arr2 = arr[i];
        if(arr2 ===)
    if(sum % 2 === 0){
        console.log("Even more so!");
    }else{
        console.log("That's odd!");
    }
}